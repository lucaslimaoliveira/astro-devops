apiVersion: v1
kind: Service
metadata:
  name: astro-demo
  labels:
    app: astro-demo
spec:
  type: ClusterIP
  selector:
    app: astro-demo
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
